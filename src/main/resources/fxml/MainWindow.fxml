<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.AnchorPane?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.VBox?>

<BorderPane xmlns="http://javafx.com/javafx/25"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="ma.farm.controller.MainWindowController"
            prefHeight="700.0" prefWidth="1200.0">

    <!-- LEFT SIDEBAR -->
    <left>
        <VBox styleClass="sidebar" prefWidth="250.0" spacing="10" style="-fx-background-color: #2c3e50; -fx-padding: 20;">

            <!-- App Title -->
            <Label text="Chicken Farm" style="-fx-text-fill: white; -fx-font-size: 20px; -fx-font-weight: bold; -fx-padding: 0 0 20 0;" />

            <!-- Navigation Buttons -->
            <Button fx:id="dashboardBtn" text="📊 Dashboard" onAction="#showDashboard"
                    prefWidth="210" prefHeight="45"
                    style="-fx-background-color: #3498db; -fx-text-fill: white; -fx-font-size: 14px; -fx-cursor: hand;" />

            <Button fx:id="chickenBayBtn" text="🏠 Chicken Bay" onAction="#showChickenBay"
                    prefWidth="210" prefHeight="45"
                    style="-fx-background-color: #34495e; -fx-text-fill: white; -fx-font-size: 14px; -fx-cursor: hand;" />

            <Button fx:id="eggsBayBtn" text="🥚 Eggs Bay" onAction="#showEggsBay"
                    prefWidth="210" prefHeight="45"
                    style="-fx-background-color: #34495e; -fx-text-fill: white; -fx-font-size: 14px; -fx-cursor: hand;" />

            <Button fx:id="storageBtn" text="📦 Storage" onAction="#showStorage"
                    prefWidth="210" prefHeight="45"
                    style="-fx-background-color: #34495e; -fx-text-fill: white; -fx-font-size: 14px; -fx-cursor: hand;" />

            <Button fx:id="tasksBtn" text="✅ Tasks" onAction="#showTasks"
                    prefWidth="210" prefHeight="45"
                    style="-fx-background-color: #34495e; -fx-text-fill: white; -fx-font-size: 14px; -fx-cursor: hand;" />

            <Button fx:id="personnelBtn" text="👨‍🌾 Personnel" onAction="#showPersonnel"
                    prefWidth="210" prefHeight="45"
                    style="-fx-background-color: #34495e; -fx-text-fill: white; -fx-font-size: 14px; -fx-cursor: hand;" />

            <!-- Logout Button at Bottom -->
            <Button text="🚪 Logout" onAction="#handleLogout"
                    prefWidth="210" prefHeight="45"
                    style="-fx-background-color: #e74c3c; -fx-text-fill: white; -fx-font-size: 14px; -fx-cursor: hand; -fx-margin-top: 100;"
                    VBox.vgrow="ALWAYS" />
        </VBox>
    </left>

    <!-- CENTER CONTENT AREA -->
    <center>
        <AnchorPane fx:id="contentArea" style="-fx-background-color: #ecf0f1;">
            <!-- Pages will be loaded here dynamically -->
        </AnchorPane>
    </center>

</BorderPane>