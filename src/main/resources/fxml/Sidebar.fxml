<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.VBox?>

<VBox xmlns="http://javafx.com/javafx"
      xmlns:fx="http://javafx.com/fxml"
      fx:controller="ma.farm.controller.SidebarController"
      prefWidth="250.0"
      spacing="10"
      style="-fx-background-color: #2c3e50; -fx-padding: 20;">

    <!-- App Title -->
    <Label text="Chicken Farm"
           style="-fx-text-fill: white; -fx-font-size: 20px; -fx-font-weight: bold; -fx-padding: 0 0 20 0;" />

    <!-- Navigation Buttons -->
    <Button fx:id="dashboardBtn"
            text="📊 Dashboard"
            onAction="#showDashboard"
            prefWidth="210"
            prefHeight="45"
            style="-fx-background-color: #3498db; -fx-text-fill: white; -fx-font-size: 14px; -fx-cursor: hand;" />

    <Button fx:id="chickenBayBtn"
            text="🏠 Chicken Bay"
            onAction="#showChickenBay"
            prefWidth="210"
            prefHeight="45"
            style="-fx-background-color: #34495e; -fx-text-fill: white; -fx-font-size: 14px; -fx-cursor: hand;" />

    <Button fx:id="eggsBayBtn"
            text="🥚 Eggs Bay"
            onAction="#showEggsBay"
            prefWidth="210"
            prefHeight="45"
            style="-fx-background-color: #34495e; -fx-text-fill: white; -fx-font-size: 14px; -fx-cursor: hand;" />

    <Button fx:id="storageBtn"
            text="📦 Storage"
            onAction="#showStorage"
            prefWidth="210"
            prefHeight="45"
            style="-fx-background-color: #34495e; -fx-text-fill: white; -fx-font-size: 14px; -fx-cursor: hand;" />

    <Button fx:id="tasksBtn"
            text="✅ Tasks"
            onAction="#showTasks"
            prefWidth="210"
            prefHeight="45"
            style="-fx-background-color: #34495e; -fx-text-fill: white; -fx-font-size: 14px; -fx-cursor: hand;" />

    <Button fx:id="personnelBtn"
            text="👨‍🌾 Personnel"
            onAction="#showPersonnel"
            prefWidth="210"
            prefHeight="45"
            style="-fx-background-color: #34495e; -fx-text-fill: white; -fx-font-size: 14px; -fx-cursor: hand;" />

    <!-- Spacer to push logout button to bottom -->
    <VBox VBox.vgrow="ALWAYS" />

    <!-- Logout Button at Bottom -->
    <Button text="🚪 Logout"
            onAction="#handleLogout"
            prefWidth="210"
            prefHeight="45"
            style="-fx-background-color: #e74c3c; -fx-text-fill: white; -fx-font-size: 14px; -fx-cursor: hand;" />
</VBox>