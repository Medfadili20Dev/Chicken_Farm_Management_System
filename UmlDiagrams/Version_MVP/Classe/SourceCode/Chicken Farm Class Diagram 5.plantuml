@startuml Diagram_5_Utility_Classes

title Diagram 5: Utility Classes

' ========================================
' UTILITY CLASSES
' ========================================

class NavigationUtil {
  --
  + {static} navigateTo(stage: Stage, fxmlPath: String): void
  + {static} showDialog(title: String, message: String, type: AlertType): void
  + {static} showConfirmation(title: String, message: String): boolean
}

class ValidationUtil {
  --
  + {static} isEmailValid(email: String): boolean
  + {static} isNotEmpty(field: String): boolean
  + {static} isPositiveNumber(value: double): boolean
  + {static} isDateValid(date: LocalDate): boolean
  + {static} isPhoneValid(phone: String): boolean
}

class DateUtil {
  --
  + {static} getDaysUntil(targetDate: LocalDate): int
  + {static} getDaysBetween(startDate: LocalDate, endDate: LocalDate): int
  + {static} calculateAge(startDate: LocalDate): int
  + {static} formatDate(date: LocalDate, pattern: String): String
  + {static} parseDate(dateString: String, pattern: String): LocalDate
  + {static} getToday(): LocalDate
  + {static} isToday(date: LocalDate): boolean
  + {static} isThisWeek(date: LocalDate): boolean
  + {static} isThisMonth(date: LocalDate): boolean
}

note right of NavigationUtil
  <b>Handles all navigation</b>
  between views using FXML
  
  Shows dialogs and alerts
  
  <b>Example usage:</b>
  NavigationUtil.navigateTo(stage, 
    "/fxml/DashboardView.fxml");
end note

note right of ValidationUtil
  <b>Centralized validation</b>
  for all user inputs
  
  Reusable across controllers
  
  <b>Example usage:</b>
  if (!ValidationUtil.isEmailValid(email)) {
    // show error
  }
end note

note right of DateUtil
  <b>Date calculations</b>
  Age, transfers, statistics
  
  Used extensively in business logic
  
  <b>Example usage:</b>
  int age = DateUtil.calculateAge(
    chicken.getArrivalDate());
end note

@enduml